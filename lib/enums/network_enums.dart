enum NetworkEnums {
  login('login'),
  introOff('introOff'),
  idToken('idToken'),
  accessToken('accessToken'),
  refreshToken('refreshToken');

  final String name;
  const NetworkEnums(this.name);
}
